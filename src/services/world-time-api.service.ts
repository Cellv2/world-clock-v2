export interface WorldTimeApiService {};
export class WorldTimeApiService {
    // get available timezones
 
    // get / set area
    // get / set region
    // get / set subregion
 
    // get time for area/region/subregion
}
